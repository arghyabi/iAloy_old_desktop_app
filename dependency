sudo apt-get install qt5-default openssl libssl-dev

git clone https://github.com/pocoproject/poco.git ~/Download/poco/

cd ~/Download/poco
sudo ./configure

sudo make && make install

sudo ldconfig

